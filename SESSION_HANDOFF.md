# SESSION_HANDOFF

**Fase attuale:** v3 — Geografia avanzata. **Fase 0, v1 Fondamenta e l'intera v2 Date flessibili sono CHIUSE e validate sul telefono reale.** Il piano v3.1→v3.7 è approvato. **v3.1 Multi-origine su Date fisse è IMPLEMENTATA, CI verde e Release pubblicata; manca solo la validazione sul telefono reale.**

**Stato completato finora:** Fase 0 infrastruttura/firma persistente = PASS. v1 Fondamenta = PASS (API key locali, prima ricerca reale, IATA anti-typo, Room cache 4h, Diagnostica/clipboard). v2.1 Weekend Discovery = PASS (`FCO→MAD`, ottobre 2026, Explore 01/10→05/10, 95 EUR, 1 query, cache PASS). v2.2 Weekend Verifica precisa = PASS (`FCO→MAD`, novembre 2026, Explore 57 EUR → Pattern B Wizz Air 54 EUR, partenza 06:00 dentro `5,11`, 0 scali; 3 query iniziali, replay cache 0). v2.3 N notti ramo SerpApi = PASS (`FCO→MAD`, 3 notti, target 25/10/2026, ±5; 11 candidate, 7 valutate; Ryanair 28→31/10, 53 EUR, 06:25→09:00, 0 scali; replay cache 0). v2.3 ramo SearchAPI.io Calendar = PASS sul telefono reale, inclusa cache. **v2 COMPLETAMENTE CHIUSA.**

**v3 architettura approvata:** sequenza obbligatoria `v3.1 → v3.2 → v3.3 → v3.4 → v3.5 → v3.6 → v3.7`. Modello geografico: origine `Airports(max 3)`; destinazione come alternativa esclusiva fra `Airports(max 3)`, `Anywhere`, `Country`. Niente destinazioni composite. Preservare `DISCOVERY → VERIFICA → DETTAGLIO`, usare supporto multi-airport nativo dei provider, cache canonicalizzate, Account API live e niente brute force del prodotto cartesiano.

**v3.1 implementazione:** solo modalità `Date fisse`. UI con 1–3 aeroporti di partenza, pulsante `+ Aggiungi origine`, origini aggiuntive rimovibili; destinazione ancora singola. Validazione locale su ogni IATA, duplicati origine bloccati, destinazione non può coincidere con un'origine. Repository normalizza/deduplica/ordina le origini e invia una sola Google Flights con `departure_id` comma-separated. Il risultato mostra `Origine effettiva: <IATA>` letta dal `departure_airport.id` del primo segmento. Cache key: lista origini ordinata alfabeticamente + destinazione + date, quindi `FCO,CIA` e `CIA,FCO` sono equivalenti. Nessuna migrazione Room: la tabella `flight_search_cache` resta invariata; il campo `departureId` già esistente memorizza l'origine effettiva vincente e resta compatibile con la vecchia cache single-origin.

**CI v3.1:** GitHub Actions run **#23 = SUCCESS**, build **`0.1.0-dev.23`**, commit `834a68b276ecf22761ca9e1c91cddb98702ba961`. `BUILD SUCCESSFUL in 1m 35s`, compile/KSP/assembleRelease, zipalign, apksigner e pubblicazione `dev-latest` tutti verdi. Firma v2/v3 valida, 1 signer, fingerprint canonico invariato `a1f432f512e3d1867ee4b4535fb06a83fae5413ee700113b34e8b926a2767df3`. APK SHA-256 `5b4bd4c369c913afef736a3765028adef992f3709e1396b99a35858d7d55b766`.

**Test telefono v3.1 consigliato:** installare `0.1.0-dev.23` sopra la build attuale senza disinstallare. Prima test locale anti-typo su seconda origine (`FCO` + `FC0` → `MAD`) e premere `Correggi`: 0 query. Poi test live con `FCO + CIA + MXP → MAD`, andata `20/11/2026`, ritorno `23/11/2026`: atteso 1 Account API gratuita + **1 sola query Google Flights**, risultato con `Origine effettiva` uguale a una delle tre origini. Infine invertire l'ordine, es. `MXP + FCO + CIA`, mantenendo destinazione/date: deve essere `CACHE HIT`, 0 nuove query provider e stesso risultato/origine vincente. Diagnostica del live: `SERPAPI_ACCOUNT` + un solo `GOOGLE_FLIGHTS`; replay: `CACHE HIT`.

**Rischio Explore da ricordare per v3.4/v3.5:** le release notes ufficiali 2026 di Google Travel Explore API mostrano fix recenti su `travel_duration` Weekend, `max_duration`, `stops` e, il 09/07/2026, un problema per cui la maggior parte delle ricerche valide poteva restituire risultati vuoti. Quando si implementeranno Anywhere/Paese, una risposta vuota/anomala non va trattata ingenuamente come “nessun volo”: aggiungere parsing/diagnostica e messaggi robusti, distinguendo quando possibile errore/anomalia provider da assenza reale di opportunità. Registrare il rischio anche in PROJECT_SPEC.md.

**Non implementato ancora:** v3.2 multi-destinazione, v3.3 multi-aeroporto N notti, v3.4 Anywhere, v3.5 Country/KGMID, v3.6 verifica geografica Weekend, v3.7 integrazioni estreme. **Non avanzare a v3.2 prima del PASS reale di v3.1.**

**Regola:** dopo ogni decisione/modifica/step completato aggiornare sia `PROJECT_SPEC.md` sia questo file. Nuova chat: leggere prima entrambi i file e confermare lo stato.
