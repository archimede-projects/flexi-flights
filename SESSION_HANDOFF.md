# SESSION_HANDOFF

**Fase attuale:** Fase 0 — infrastruttura GitHub-only.

**Stato:** il progetto Compose minimo VolaFlex è stato scritto nella repo privata `archimede-projects/flexi-flights`; la CI/Release deve ancora essere aggiunta e verificata.

**Ultimo step completato:** creati Gradle/settings, modulo app, manifest e `MainActivity.kt` con package `com.archimedeprojects.volaflex` e schermata `VolaFlex - Build OK`.

**Prossimo step immediato:** aggiungere `.github/workflows/android-build.yml`, fare partire `Android Build`, leggere i log e correggere finché il job è verde e la Release `VolaFlex - Development latest` contiene `VolaFlex-dev.apk`.

**Problemi aperti:** prima build non ancora verificata; Release/APK non ancora verificati; APK non ancora installato sul telefono; firma debug stabile da aggiungere dopo la validazione della pipeline.

**Regola:** dopo ogni decisione, modifica o step completato aggiornare sia `PROJECT_SPEC.md` sia questo file.

**Nuova chat:** leggi `SESSION_HANDOFF.md` e `PROJECT_SPEC.md` dalla repository, poi conferma di aver capito lo stato prima di procedere.

**Promemoria:** leggi `PROJECT_SPEC.md` per il contesto completo prima di rispondere o prendere decisioni tecniche.
